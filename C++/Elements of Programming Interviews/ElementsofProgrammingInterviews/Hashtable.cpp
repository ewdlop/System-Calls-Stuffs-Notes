#include <numeric>
#include <string>

//int StringHash(const std::string& s, int modulus)
//{
//	const int kMult = 997;
//	return std::accumulate(begin(s), end(s), 0, [kMult, modulus](int val, char c) { (val * kMult + c) % modulus;
//	});
//}